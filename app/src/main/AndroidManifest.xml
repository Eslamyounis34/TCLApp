<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tclapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@drawable/applogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/applogo"
        android:usesCleartextTraffic="true"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.CalculatorClass" />
        <activity android:name=".Activities.CountryInfoActivity" />
        <activity android:name=".Activities.CleaningProductActivity" />
        <activity android:name=".Activities.SearchResult" />
        <activity android:name=".Activities.ProcedurePdfActivity" />
        <activity android:name=".Activities.Register" />
        <activity android:name=".Activities.ProductAllDetails" />
        <activity android:name=".Activities.ImprintActivity" />
        <activity android:name=".Activities.ConditionsOfUseActivity" />
        <activity android:name=".Activities.ControlActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.ProductsActivity" />
        <activity android:name=".Activities.ProcedureActivity" />
        <activity android:name=".Activities.CleaningActivity" />
        <activity android:name=".Activities.ConsultantActivity" />
        <activity android:name=".Activities.MainActivity">

        </activity>
    </application>

</manifest>